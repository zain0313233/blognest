[build]
  # prisma generate runs via postinstall + build script; needs DATABASE_URL in Netlify env
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
